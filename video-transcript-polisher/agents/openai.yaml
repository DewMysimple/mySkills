interface:
  display_name: "Video Transcript Polisher"
  short_description: "Conservatively polish ASR transcripts with semantic review"
  default_prompt: "Use $video-transcript-polisher to assess the task, wait for explicit confirmation, process each confirmed file once, and perform semantic paragraph-quality review with long-block checks before writing."
